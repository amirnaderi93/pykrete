{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "Dathon",
  "scopeName": "source.dathon",
  "comment": "dathon is a strict superset of Python, so `.dpy` files are highlighted with Python's TextMate grammar. This grammar simply delegates to `source.python` — the grammar VS Code ships built-in. dathon-lsp layers semantic tokens on top for schema-aware coloring.",
  "patterns": [{ "include": "source.python" }]
}
