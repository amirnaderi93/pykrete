---
title: Playground
description: Try pykrete in your browser. Edit `.pyk` code and watch pykrete's schema-aware diagnostics, hover, completion, and go-to-definition update as you type — running locally via WebAssembly.
---

import Playground from '../../components/Playground.tsx';

Edit the source below. pykrete runs in your browser via WebAssembly —
the same checker the CLI and the VS Code extension use. No code leaves
your machine.

<Playground client:only="react" />

## What you're seeing

Every keystroke (debounced ~300 ms) runs the source through pykrete's
single-file analyzer. Diagnostics show up as squiggles in the editor
and as one-liners in the panel below — each carrying pykrete's
`D\d\d\d\d` code and rule name.

The playground wires pykrete into Monaco for the IDE features that
matter on a `.pyk` file:

- **Schema-aware diagnostics.** Column typos, unknown schemas, type
  mismatches, and the rest of the `D00xx` rule surface — same engine
  the VS Code extension runs.
- **Hover.** Hover a Schema class name to see its fields; hover a
  DataFrame-typed identifier to see the bound schema; hover a Spark
  method (`sales.select`, `F.sum`) to see its real pyspark signature
  and docstring summary.
- **Completion.** Type a column name inside `col("…")` to get
  pykrete's column suggestions from the bound schema. Type `sales.`
  on a `DataFrame[Schema]`-typed local to get the DataFrame method
  surface; `F.` to get `pyspark.sql.functions`; `.groupBy("…").` to
  get GroupedData methods. The Spark suggestions are generated at
  build time from a real pyspark install — see
  `docs-site/scripts/gen-pyspark-symbols.py` in the repo.
- **Go-to-definition.** Cmd/Ctrl+click (or F12) on a `DataFrame[X]`
  reference to jump to `class X(Schema):`.

## What's NOT here

This is a single-file pykrete sandbox. It is **not** a general Python
type checker:

- No general Python type checking. `int`/`str`/`bool` and the rest of
  Python's standard library are not validated here — pykrete focuses
  on schema and DataFrame semantics. For full Python typing, install
  the [VS Code extension](/pykrete/getting-started/install/) — it
  runs pykrete alongside basedpyright (or pyright), so you get both.
- No cross-file resolution. Multi-file imports, project
  configuration, and the `.pyk-import` mechanism need real file
  system access — use the CLI for those.
- `Schema`, `DataFrame`, `col`, `F`, and the lowercase PySpark-style
  type aliases (`string`, `double`, `long`, …) are pre-declared for
  you here so you can start typing without import boilerplate. Real
  projects use explicit imports — see the Install page.

## Limits

- **No share-via-URL yet.** Refresh discards your edits. Bookmarkable
  snippet URLs are on the roadmap.
- **Desktop-first.** Monaco editor is hard to use on a phone; the
  diagnostics list is still useful on small screens, but typing is
  not.
- **Cold-start bundle.** Monaco loads from a CDN (~2 MB); pykrete-wasm
  is a few hundred KB and loads on demand when this page mounts.
  Other pages on the docs site are unaffected.

## Want it locally?

```bash
pip install pykrete
pykrete check path/to/your/file.pyk
```

The [Install](/pykrete/getting-started/install/) page walks through
the full editor setup (LSP for autocomplete and hover, the VS Code
extension, project configuration).
