---
title: Production readiness
description: Stability commitments, false-positive policy, release cadence, and known limitations for production PySpark teams evaluating pykrete.
---

## TL;DR

pykrete is feature-complete for PySpark as of the [v0.1 release line](https://github.com/amirnaderi93/pykrete/releases). v1.3 added pandas check-site coverage via the `PandasFrame[X]` dialect dispatch; v1.4 added depth on pandas — 7 new pandas-heavy donors (scikit-learn, statsmodels, pandera, Great Expectations, prophet, seaborn, yfinance), 21 pandas `PROBE-TYPE-IS` markers proving dtype tracking on `PandasFrame[X]`, and three checker bug closures. v1.5 added **cross-dialect handoff**: `df.toPandas()` re-tags `SparkFrame[X]` to `PandasFrame[X]`; `spark.createDataFrame(pdf)` re-tags back when a schema source is present; the round-trip path preserves the tag; pandas `.head()` / `.tail()` / `.first()` are dialect-gated; `.loc[:, "col"]` literal-form lands; `pykrete check --report-aliases` emits the v2.0 migration-sizing envelope. v1.6 ships **`pykrete migrate`**: the auto-rewriter binary that walks each `DataFrame[X]` binding's downstream usage, classifies it as Spark / pandas / ambiguous via call-graph adjudication, and rewrites the annotation in place to the dialect-tagged canonical name (atomic per file, token-preserving). Paired atomically with D0090 strict-mode escalation: under `"typeCheckingMode": "strict"` the warning lands as **error**, but the fix-button ships in the same release. The pandas reshape downpayment ships pandas `pivot_table(index=, columns=, values=, aggfunc=)` literal-form column checking. The v1.5 `.take()` dialect-gate deferral closes; the `pdf.loc[mask, "col"]` nested-arg D0030 FP closes; the audit-debt `cross_dialect_handoff_gate` recognizer is extracted. v1.10 adds **v2.0 migration archivability**: `pykrete check --deprecation-report --snapshot=<path>` writes the v2 envelope to disk for prior-report caching across releases, and `--fail-on-nonempty` replaces the `jq | test` boilerplate as a first-class CI gate. D0091 surface completes — 8 new properties total (`na` / `write` / `writeStream` / `storageLevel` on the Spark side; `index` / `values` / `shape` / `T` on the pandas side) — and pandas `df.stack(level=, dropna=)` lands as the v1.10 reshape arm. v1.11 ships **pandas `df.unstack(level=, fill_value=)` literal-form** (mirror of v1.10's `stack` arm), **cross-codebase property probes for the v1.10 D0091 8-property surface** (pykrete-tests PR-P1 #39 — closes the v1.10 PR-D1 carve-out), and the **audit-tooling block**: `scripts/trust-claim-sweep-checklist.sh`, `scripts/changelog-cite-check.sh`, and `.github/workflows/auto-label-release-pr.yml`. v1.12 closes the **v1.11 calendared GITHUB_TOKEN promise** — the auto-label workflow now dispatches `release-gate.yml` via the `actions.createWorkflowDispatch` API, bypassing GitHub's GITHUB_TOKEN cross-workflow no-trigger rule so the release-gate fires non-skipped on labeled PR events end-to-end. v1.12 also lands **D0080 returnTypeMismatch cross-codebase trust coverage** (pykrete-tests PR-P1 #42), closing the longest-standing trust gap since v1.6. Plus **pandas `pivot_table(aggfunc=)` 11-string allowlist recognition** (informational; primes v1.13+ aggfunc-driven inference) and the **multi-line ack-marker rationale block** per spec §6.1.4. v1.13 closes the longest-standing 7-cycle correctness gap with the **D0080 dialect-on-return checker arm** + **`pivot_table(aggfunc=)` Derived-schema synthesis** (first observable aggregate-semantics-informed schema inference) + backtick-preservation tripwire + dispatched-run required-status-check. v1.14 closes **D0080's constructor carve-out** (cross-dialect constructor returns now fire, plus a multi-clause format when both dialect and column-type mismatches land on the same return) + ships **`groupby.agg` Derived synthesis** as the sibling to v1.13's `pivot_table(aggfunc=)` + lands **`--compare-to` snapshot diff** (SIMPLE three-bucket envelope; exit-nonzero on `added`; mutex with `--ack` / `--snapshot` / `--fail-on-nonempty`) + the envelope schema v2 **provenance pair** (`pykreteSourceCommit` + `generatedAt`) round-trips through CI artifacts. v1.15 extends **pandas chain-depth** through `groupby.agg().reset_index(drop=True)` + `set_index([literal-keys])` so the v1.14 `Derived` envelope survives one more transform, closes the **v1.14 synthesis-arm cross-codebase coverage gaps** (positive `groupby.agg` probes + negative D0080 constructor-return probes in pykrete-tests PR-P1 #50), consolidates the dtype-override family behind a shared **`resolve_override_ty` primitive** in preparation for the window-aggregation arms (the `Windowed` lattice variant was evaluated and rejected in v1.16 in favor of direct-chain recognition; deferred to v1.17), and ships **marketing-table gate v3** (fence-vs-claim discipline for `<num> <key>` markdown-table cells; closes the v1.14 audit-tooling carve-out). v1.16 extends pandas reshape to **time/window aggregation** — `resample.agg` and `rolling.agg` direct chains plus the dict and callable forms of `groupby.agg` — and turns four previously over-claiming shapes into honest declines (named-aggregation `groupby(k).agg(out=(col, fn))`, which had been a **false positive** since v1.14; `rolling` over a non-numeric column; numeric-restricting aggregation over a non-numeric column, on `groupby.agg` and `resample.agg` alike; and `resample(<rule>, on=<col>)`). The rationale is the same in the first three: pandas 2.x **raises** rather than silently dropping the column the way pre-2.0 pandas did, and pykrete does not synthesize a schema for code that errors at runtime. The fourth is the same principle applied to shape rather than dtype — pandas moves the `on=` column into the resample index, so keeping it claimed a column the result doesn't have; `rolling(<n>, on=<col>)` keeps the column and is deliberately unaffected. `reset_index(inplace=True)` / `set_index(inplace=True)` now resolve to `None`, matching pandas. A deliberate "degrade to Unknown rather than fabricate" policy keeps the checker honest: when pykrete can't determine a schema or a type with confidence, it stops checking that subtree rather than guessing. A real-codebase integration loop ([pykrete-tests](/about/pykrete-tests/)) catches regressions before they ship.

For the trust posture behind the engineering — why pykrete cannot break a production pipeline, and how each release is validated — see the [Reliability and trust](https://github.com/amirnaderi93/pykrete#reliability-and-trust) section of the README.

## Stability commitments

Once a piece of surface ships in a release, the project commits to backward-compatible behavior on the following:

- **Schema declaration syntax.** `Schema` classes, `Optional[T]` for nullable columns, the `Array` / `Map` / struct-class nested-type forms, and the TypeScript-style schema operators (`Pick`, `Omit`, `Merge`).
- **The dataframe annotation surface.** `SparkFrame[Schema]` and `PandasFrame[Schema]` (canonical, v1.3+), and `DataFrame[Schema]` (deprecated alias for `SparkFrame[Schema]`, stable through v1.x and removed in v2.0). Variable annotations, function parameter and return types, `.cast(SparkFrame[Schema])` re-anchors.
- **Diagnostic codes.** `D0001`, `D0010`, `D0011`, `D0020`, `D0021`, `D0030`, `D0040`, `D0050`, `D0051`, `D0060`, `D0070`, `D0071`, `D0072`, `D0073`, `D0080`, `D0081`, `D0082`, `D0083`, `D0084`, `D0090`, `D0091`. The numeric code and the rule name are part of the contract; the diagnostic message text is not. `D0090 deprecatedDataFrameAlias` is new in v1.3 and warns when `DataFrame[X]` is used as the dialect-less alias for `SparkFrame[X]`; the alias is removed in v2.0. `D0091 crossDialectMethodMismatch` is new in v1.8 and warns when a pandas-only method is called on a `SparkFrame[X]` receiver (or vice versa); v1.9 matures the rule with strict-mode escalation (warning by default, error under `"typeCheckingMode": "strict"`) and a bare-attribute inference arm; v1.10 completes the property surface (4 new Spark-direction properties — `na`, `write`, `writeStream`, `storageLevel`; 4 new pandas-direction properties — `index`, `values`, `shape`, `T`); v1.11 lands the matching cross-codebase property probes (pykrete-tests PR-P1 #39). `D0080 returnTypeMismatch` shipped in v1.0 at error severity; v1.12 lands its cross-codebase trust coverage via pykrete-tests PR-P1 #42 (longest-standing gap since v1.6 closed); v1.13 lands the **dialect-on-return checker arm** (`-> SparkFrame[X]` returning a `.toPandas()` chain now fires the rule at error severity with a dedicated dialect-mismatch message), closing the longest-standing 7-cycle correctness gap. v1.14 closes the v1.13 honest-silence carve-out — `-> SparkFrame[X]` returning `pd.DataFrame({...})`, `-> PandasFrame[X]` returning `spark.read.parquet(path)`, and `spark.createDataFrame(rows, schema=<Schema>)` with a disagreeing annotation all now fire D0080; when both dialect and column-type mismatches land on the same return, D0080 emits a single multi-clause message (one fire, two clauses) rather than two stacked fires. v1.15 extends D0030 firing through pandas chain-depth: `pdf.groupby("k").agg("sum").reset_index(drop=True)` now keeps the synthesized `Derived` envelope alive (downstream `result["typo"]` fires D0030 instead of degrading to Unknown), and `pdf.set_index([literal-keys])` removes the named literal-key columns from the accessible schema (downstream `result["literal_key"]` fires D0030 instead of going silent).
- **`pykrete.json` keys.** `typeCheckingMode`, `exclude`, `rules`. New keys may be added; existing ones won't change shape.
- **The CLI's machine-readable output** (`pykrete check --format json`) and exit codes. Shipped in [v0.1.33](https://github.com/amirnaderi93/pykrete/releases/tag/v0.1.33); the JSON schema becomes a stability contract at v1.0.0 (breaking changes after that point require a SemVer major bump). Exit codes are also part of the contract: `0` when no diagnostics, `1` when any diagnostic fires (error _or_ warning — matches the text format and lets CI scripts react uniformly to warnings like `D0072 duplicateSchemaName`). A future `--max-severity` flag may let consumers customize this; tracked in `docs/design/spark-coverage.md`.

### JSON output stability contract

The `--format json` payload carries an explicit `schemaVersion` field (currently `"1"`). Consumers pin to that; the pykrete `version` is informational. The contract covers:

- **JSON field names — STABLE.** Renaming a field requires a SemVer-major bump and a `schemaVersion` bump.
- **JSON field types — STABLE.** Changing a string to an integer (or similar) requires a SemVer-major bump.
- **JSON field semantics — STABLE.** Changing what a field means requires a SemVer-major bump.
- **D-code identity — STABLE.** `D0030` will always mean `unknownColumn`; codes are never reassigned.
- **Diagnostic message wording — NOT STABLE.** Rewording for clarity is a SemVer-minor change. Consumers should match on `code` / `ruleName` / `severity`, not on message text.
- **Adding a new top-level or per-diagnostic field — NON-BREAKING.** Consumers must accept unknown fields. `schemaVersion` stays at `"1"`.
- **Adding a new severity — NON-BREAKING.** Consumers must handle unknown severities gracefully (a sensible default is to treat unknown as `error`). `schemaVersion` stays at `"1"`.
- **Adding a new D-code — NON-BREAKING.** Consumers must handle unknown codes gracefully. `schemaVersion` stays at `"1"`.

Bumping `schemaVersion` to `"2"` only happens alongside a SemVer-major pykrete release.

What may still change without notice:

- The internal LSP wire protocol with the embedded Python engine (today's multiplexer is interim — see the [roadmap](/about/roadmap/#forking-ty)).
- The wasm API surface (`pykrete-wasm`): shipped in v0.1.16 and consumed by the in-browser [playground](/playground/). The current export shape (`check_source`, `hover_at`, `complete_at`, `definition_at`) is stable in spirit until v1.0.0 and becomes part of the SemVer contract from v1.0 onward. The crate is a single-file analyzer wrapper, not a general-purpose embedding library — multi-file / cross-import support stays a CLI / LSP capability.

## False-positive policy

**No false positives.** When pykrete can't determine a schema or a column's type with confidence, it degrades that subtree to Unknown rather than guess. Two concrete examples from the v0.1 surface:

- `spark.read.parquet("s3://...")` returns Unknown until the user re-anchors with `.cast(SparkFrame[Schema])` or a typed variable annotation. The schema is genuinely runtime data; pykrete won't invent one.
- `F.struct(F.lit(1))` falls back to positional names (`col1`, `col2`, …) when no `.alias("x")` is present, rather than fabricating a guessed field name. Heterogeneous value types in `melt` / `unpivot` degrade the value-column type to Unknown rather than picking a "winner".

The same rule applies at the generic-inference layer: a TypeVar bound to incompatible schemas across argument slots stays Unknown. Downstream checks against an Unknown subtree are permissive: no diagnostic fires unless the user re-anchors.

A static checker that cries wolf gets switched off; pykrete prefers to stay quiet when it isn't sure.

## Release cadence

The Spark-coverage closure sprint (v0.1.7 onward, May 2026) ran at multiple releases per week — the finishing pass on the v1.0.0 surface, not a steady-state cadence. v1.0.0 shipped, v1.1/v1.2 added the schema-tracking probe suite and PROBE-TYPE-IS, v1.3 added pandas check-site coverage, v1.4 added pandas depth (7 new donors + pandas type-tracking probes), v1.5 added cross-dialect handoff between Spark and pandas, v1.6 shipped `pykrete migrate` paired with D0090 strict-mode escalation plus pandas `pivot_table` literal-form, v1.7 hardens the migrator UX (`--check` is now the default; `--apply` opts into the rewrite) + ships pandas `melt` literal-form + extracts the `dialect_signals` shared module + closes the Spark-D1 audit-debt with 14 new `SPARK_DISCRIMINATORS`, v1.8 ships the v2.0 deprecation runway (`pykrete check --deprecation-report` JSON envelope + D0090 message amend) + the spark-D2 D0091 cross-dialect mismatch warning + two audit-class structural closures (`build.rs`-generated inventory and CHANGELOG-binary CI gate), v1.9 ships v2.0 migration plannability (`--deprecation-report` v2 envelope with per-site `migrationStatus` + `--ack` filter) + D0091 maturity (strict-mode escalation + suggestion drift guard + `shape_changes` hint + NEW bare-attribute inference arm) + CI-running tests behind the v1.8 `build.rs` inventory tripwire + a `text-numeric` CHANGELOG gate that live-verifies numeric trust-claims, and v1.10 ships v2.0 migration archivability (`--snapshot=<path>` file-write + `--fail-on-nonempty` CI gate) + D0091 surface completion (8 new properties — `na` / `write` / `writeStream` / `storageLevel` on the Spark side, `index` / `values` / `shape` / `T` on the pandas side) + pandas `df.stack(level=, dropna=)` literal-form + closes the v1.9 architecture audit-debt class (ack-marker multi-line signatures, property / method tripwire, release-gate CI workflow, CHANGELOG grep gate v3 prose scan) + promotes the v1.9 §9.2 centralized version-bump trial to standing practice, v1.11 ships pandas `df.unstack(level=, fill_value=)` literal-form + cross-codebase property probes for the v1.10 D0091 8-property surface (pykrete-tests PR-P1 #39) + the audit-tooling block (trust-claim sweep checklist, CHANGELOG cite-check, auto-label workflow), v1.12 closes the v1.11 GITHUB_TOKEN calendared promise (auto-label workflow now dispatches `release-gate.yml` via `actions.createWorkflowDispatch`) + lands D0080 returnTypeMismatch cross-codebase trust coverage (closes the longest-standing gap since v1.6) + pandas `pivot_table(aggfunc=)` 11-string allowlist recognition (priming v1.13+ aggfunc-driven inference) + multi-line ack-marker rationale block per spec §6.1.4 + release-gate `cargo test` memoization, v1.13 closes the longest-standing 7-cycle correctness gap with the D0080 dialect-on-return checker arm + pandas `pivot_table(aggfunc=)` Derived-schema synthesis (first observable aggregate-semantics-informed schema inference) + backtick-preservation tripwire + dispatched-run required-status-check workflow, v1.14 closes the D0080 constructor carve-out (cross-dialect constructor returns now fire; multi-clause format when both dialect and column-type mismatches land on the same return) + ships `groupby.agg` Derived synthesis (sibling to v1.13's `pivot_table(aggfunc=)`, shared inference table) + `--compare-to` snapshot diff (SIMPLE three-bucket envelope, exit-nonzero on `added`) + envelope schema v2 provenance pair (`pykreteSourceCommit` + `generatedAt`), and v1.15 extends pandas chain-depth through `groupby.agg().reset_index(drop=True)` + `set_index([literal-keys])` (the v1.14 `Derived` envelope survives one more transform) + closes the v1.14 synthesis-arm cross-codebase coverage gaps (pykrete-tests PR-P1 #50) + consolidates the dtype-override family behind a shared `resolve_override_ty` primitive + ships marketing-table gate v3 (audit-tooling fence-vs-claim discipline). The cadence is now a more measured per-feature pace as the focus moves to broader pandas reshape, the `.query` / `.eval` mini-DSLs, and polars. See the [GitHub Releases page](https://github.com/amirnaderi93/pykrete/releases) for the full per-release history.

## Real-codebase testing

Every release is regression-tested against **`171 fixtures` from `17 donors`** (58 annotated + 113 deliberately-corrupted under `probes_negative/`). The 10 PySpark donors cover the dominant Spark stack: Apache Spark, Delta Lake, Apache Iceberg ([iceberg-python](https://github.com/apache/iceberg-python)), Apache Hudi, MLflow, Feast, Kedro ([kedro-plugins](https://github.com/kedro-org/kedro-plugins)), [quinn](https://github.com/MrPowers/quinn), [dbt-spark](https://github.com/dbt-labs/dbt-spark), and [python-deequ](https://github.com/awslabs/python-deequ). The 10 pandas-coverage donors split into three honest scoping classes: **3 hybrid** (MLflow, Feast, iceberg-python) carry pandas fixtures on top of their Spark coverage; **3 direct-dispatch** ([prophet](https://github.com/facebook/prophet), [seaborn](https://github.com/mwaskom/seaborn), [yfinance](https://github.com/ranaroussi/yfinance)) annotate the actual upstream library code where pykrete's dispatched-shape recognizers match real call sites; **4 canonical-fixture-only** ([scikit-learn](https://github.com/scikit-learn/scikit-learn), [statsmodels](https://github.com/statsmodels/statsmodels), [pandera](https://github.com/unionai-oss/pandera), [Great Expectations](https://github.com/great-expectations/great_expectations)) ship user-pattern fixtures inspired by each library's API — the upstream code itself operates at numpy / metric layers above raw pandas dispatch, so the fixtures stand in for what a real user writes at the pandas boundary. See [Real-codebase tests](/about/pykrete-tests/) for the methodology and the [pykrete-tests README](https://github.com/amirnaderi93/pykrete-tests#the-donors) for the per-donor matrix. Each push rebuilds pykrete fresh from the pinned source-commit, re-runs `pykrete check` against every fixture, and JSON-diffs the output against the committed golden — any drift fails the build before it gets released.

On top of the golden-diff suite, we run **`312 probes`** that verify pykrete actually tracks columns through real transforms. The probes cover the 170 probe-bearing fixtures of the 171 vendored (57 annotated + 113 negative; the one remaining annotated fixture is a probe-free streaming fixture with no typed-DataFrame slot to anchor to): `195 positive` probes assert columns resolve cleanly after `.select` / `.filter` / `.withColumn` and the pandas analogues, AND that dtype claims on `SparkFrame[X]` / `PandasFrame[X]` parameters survive dispatched chains (24 `PROBE-TYPE-IS` markers across 10 of the `17 donors` — the v1.2 Spark side and the v1.4 pandas side, closing pykrete-tests#14); `117 negative` probes across the 113 deliberately-corrupted fixtures under `probes_negative/` assert specific diagnostics — D0030 `unknownColumn` (v1.7 widens to pandas `melt` literal-arg typos with the matching cross-codebase negative probe shipped in pykrete-tests PR-D1 #28), D0040 / D0050 / D0051 (cross-codebase coverage added in v1.7 per pykrete-tests PR-P1 #27), D0060 `missingJoinKey`, D0073 `transformInputMismatch` (cross-codebase coverage added in v1.8 per pykrete-tests PR-P1 #30), D0080 `returnTypeMismatch` (cross-codebase coverage added in v1.12 per pykrete-tests PR-P1 #42, closing the longest-standing trust gap since v1.6), D0081 `nonNumericArithmetic` (v1.4 widens to subscript-on-name receivers), D0082 `crossTypeComparison` (widened correspondingly), D0083 `nullabilityMismatch` (cross-codebase coverage added in v1.8 per pykrete-tests PR-P1 #30), D0084 `enumValueMismatch`, D0090 `deprecatedDataFrameAlias`, and D0091 `crossDialectMethodMismatch` (cross-codebase coverage added in v1.9 per pykrete-tests PR-P1 #32 on pandera + delta, extended in v1.10 PR-P1 #34 with D0091 strict-mode escalation + bare-attribute + shape-changes probes on `mlflow` / `dbt-spark` / `pandera` / `delta`, extended again in v1.11 PR-P1 #39 with 8 property probes covering the v1.10 PR-D1 surface) — actually fire. Together, the suite verifies four properties on every release: **column resolution + diagnostic firing + Spark type tracking + pandas type tracking**.

We verify enum value vocabularies in 3 of the `17 donors`: Delta CDC `_change_type` (`{"insert", "update_preimage", "update_postimage", "delete"}`), Hudi `_hoodie_operation` (`{"I", "-U", "U", "D"}`), and MLflow run status (`{"RUNNING", "FINISHED", "FAILED", "KILLED", "SCHEDULED"}`). Positive probes assert in-vocab literals stay clean across `==` / `.isin` / `withColumn` / `F.expr` / `groupBy` chains; negative probes assert D0084 fires on off-vocab typos.

**New in v1.10: v2.0 migration archivability + D0091 surface completion + pandas `stack` reshape arm + v1.9 audit-debt class closure.** The v1.10 headline is **`pykrete check --deprecation-report --snapshot=<path>` makes v2.0 migration archivable in CI**: the v2 envelope is written to disk via atomic tempfile-plus-rename (nanosecond-suffixed temp name to avoid concurrent-writer collision, cleanup-on-error guard across every error path), so CI can persist a prior-report cache and diff between releases. `--fail-on-nonempty` exits non-zero when the envelope's `sites` array is non-empty — drop `pykrete check --deprecation-report --fail-on-nonempty src/` into CI to fail on any unacked site, replacing the `jq '.sites | length' | test ... -eq 0` boilerplate adopters were writing by hand. D0091 surface completes: `SPARK_DISCRIMINATOR_PROPERTIES` gains `na`, `write`, `writeStream`, `storageLevel` (closes v1.9 spark-I1); `PANDAS_INHERITED_PROPERTIES` gains `index`, `values`, `shape`, `T` (closes v1.9 spark-I2) — both via the v1.9 bare-attribute path on `Expr::Attribute`. Pandas `df.stack(level=, dropna=)` literal-form lands as a NEW inference arm (continuing the one-reshape-arm-per-cycle cadence from v1.6 `pivot_table` and v1.7 `melt`); receiver-dialect-gated to fire only on `PandasFrame[X]` receivers because Spark's `stack` is a column-free-function, not a method. v1.10 closes the v1.9 architecture audit-debt class structurally: ack-marker multi-line signature support (the v1.9 walker silently skipped past `def foo(` when the signature ran onto a continuation line; v1.10 lands an indentation-aware walker + decorator-stack skip), property / method tripwire (build-time invariant: `SPARK_DISCRIMINATOR_PROPERTIES ⊂ SPARK_DISCRIMINATORS`; mirror for `PANDAS_INHERITED_PROPERTIES ⊂ PANDAS_ONLY_SIGNALS`), release-gate CI workflow (full CHANGELOG grep gate including the live-extract step against the `pykrete-tests` sibling checkout runs on release-PR branches), and CHANGELOG grep gate v3 prose scan (extends the v1.9 v2 gate to scan prose paragraphs for `<digit> <known-key>` patterns; single-backtick-wrapped numbers are the editor's escape hatch).

**New in v1.11: pandas `unstack` reshape arm + cross-codebase property probes (closes the v1.10 PR-D1 carve-out) + audit-tooling block.** Pandas `df.unstack(level=, fill_value=)` literal-form lands as the v1.11 reshape arm — mirror of v1.10 `stack`, continuing the one-arm-per-cycle cadence. Cross-codebase property probes for the v1.10 PR-D1 D0091 8-property surface (`na`, `write`, `writeStream`, `storageLevel`, `index`, `values`, `shape`, `T`) ship in pykrete-tests PR-P1 #39 — the v1.10 carve-out closes. The audit-tooling block lands: `scripts/trust-claim-sweep-checklist.sh` (docs-vs-history sweep against prior-release CHANGELOG pins; closes v1.10 retro rules 1+7), `scripts/changelog-cite-check.sh` (resolves CHANGELOG `path:LINE` citations against the working tree; closes v1.10 retro rule 3), and `.github/workflows/auto-label-release-pr.yml` (auto-applies `release-ready` label to PRs titled `chore(release):` or branched `release/v*`; closes v1.10 retro rule 8). Devs run the sweep checklist locally before opening PR-F; CI-side release-gate label-trigger wiring tracked for v1.12.

**New in v1.12: D0080 cross-codebase trust + GITHUB_TOKEN calendared promise closure + `pivot_table(aggfunc=)` allowlist.** v1.12 closes the v1.11 GITHUB_TOKEN calendared promise: the auto-label workflow now dispatches `release-gate.yml` via the `actions.createWorkflowDispatch` API, bypassing GitHub's GITHUB_TOKEN cross-workflow no-trigger rule. D0080 returnTypeMismatch cross-codebase trust coverage lands (pykrete-tests PR-P1 #42), closing the longest-standing trust gap since v1.6 (dialect-on-return is a checker carve-out deferred to v1.13). Pandas `pivot_table(aggfunc=)` literal-form recognition lands against an 11-string allowlist (`sum` / `mean` / `count` / `min` / `max` / `median` / `std` / `var` / `first` / `last` / `nunique`); recognition is informational (no diagnostic; result schema unchanged) and primes v1.13+ aggfunc-driven inference. Multi-line ack-marker rationale block per spec §6.1.4 — `# pykrete: ack-deprecation` (shape b) now extends acknowledgement to the entire contiguous comment block above the anchor; v1.10 adopters that depended on the strict-single-line semantic should update. Release-gate runner perf: `cargo test --release --workspace` is memoized via `PYKRETE_TESTS_COUNT_FILE` (gate step reads the memoized count rather than re-invoking), dropping total release-gate cold-cache runtime from ~70min to ~35min.

**New in v1.13: D0080 dialect-on-return arm + pivot_table aggfunc Derived synthesis + audit + CI tooling.** v1.13 closes the longest-standing 7-cycle correctness gap with the D0080 dialect-on-return checker arm: a function annotated `-> SparkFrame[X]` returning a `.toPandas()` chain (or any expression resolving to a `PandasFrame[…]`) now fires D0080 at error severity with a dedicated dialect-mismatch message. Pandas `pivot_table(aggfunc=)` graduates from informational allowlist recognition to **Derived-schema synthesis** — the first observable aggregate-semantics-informed schema inference: when `aggfunc=` is a recognized string and `values=` carries one or more literal columns, the result schema synthesizes a `Derived` envelope with the named columns at the aggregate-driven dtype (`count` / `nunique` → int64; `mean` / `std` / `var` / `median` → float64; `sum` / `min` / `max` / `first` / `last` preserve the receiver column's type). Multi-values + `columns=` (MultiIndex result) falls through to Unknown. The **backtick-preservation tripwire** (PR-A1) prevents the 2-cycle backtick-strip regression class identified in the v1.12 retro from recurring silently. The **dispatched-run required-status-check + cancel-self workflow** (PR-A2) wires the release-gate's dispatched run as a required status check on `chore(release):` PRs and retires the v1.12 polling-step. The **vscode CHANGELOG per-section masking test lock-in** (PR-V1) pins the v1.12 CHANGELOG-cite carve-out behavior. **Adopters with code that incorrectly cross-converts dialects at function boundaries, or that accidentally accessed non-`values=` columns post-`pivot_table`, will see new D0080 / D0030 fires.** Cross-codebase coverage: 279 → `289 probes` across `148 fixtures` from `17 donors`.

**New in v1.14: D0080 constructor carve-out closes + `groupby.agg` Derived synthesis + `--compare-to` snapshot diff + envelope schema v2 provenance pair.** v1.14 closes D0080's last carve-out: `-> SparkFrame[X]` returning `pd.DataFrame({...})`, or `-> PandasFrame[X]` returning `spark.read.parquet(path)`, now fires D0080, and `spark.createDataFrame(rows, schema=<Schema>)` fires when the dialect disagrees with the annotation. When both dialect and column-type mismatches land on the same return, D0080 emits a single **multi-clause** message (one fire, two clauses) rather than two stacked fires — CI greps for "two D0080 fires at the same range" need to adjust. Pandas `df.groupby(...).agg("sum")` synthesizes a `Derived` envelope mirroring v1.13's `pivot_table(aggfunc=)` arm — the synthesis helper is shared (`count` / `nunique` → int64; `mean` / `std` / `var` / `median` → float64; `sum` / `min` / `max` / `first` / `last` preserve). v1.14 ships the single-string form; the dict and callable forms land in v1.16. The new `pykrete check --deprecation-report --compare-to=<prior.json>` flag emits a SIMPLE three-bucket diff (`added` / `removed` / `unchanged`) with exit-nonzero on `added`; mutually exclusive with `--ack` / `--snapshot` / `--fail-on-nonempty` (the gating modes vs the delta-reporting mode don't compose). The v2 envelope extends with a **provenance pair** — `pykreteSourceCommit` (in-tree commit hash recorded at report time) + `generatedAt` (ISO-8601 timestamp) — round-tripped through `--snapshot` and propagated to `--compare-to` so CI snapshot artifacts carry author-pinpointing metadata across release windows. **Adopters with cross-dialect constructor returns at function boundaries, or that accidentally accessed non-aggregated columns post-`groupby.agg`, will see new D0080 / D0030 fires.** Cross-codebase coverage: `289` → `299 probes` across `158 fixtures` from `17 donors`.

**New in v1.15: pandas chain-depth extends through `reset_index(drop=True)` + `set_index([literal-keys])` + synthesis-arm cross-codebase coverage closes + `resolve_override_ty` primitive + marketing-table gate v3.** v1.15 extends the v1.14 `groupby.agg` `Derived` synthesis through one more transform: `pdf.groupby("k").agg("sum").reset_index(drop=True)` now keeps the synthesized envelope alive (the keys + aggregate columns remain accessible), and `pdf.set_index([literal-keys])` removes the named literal-key columns from the accessible schema rather than degrading to Unknown. Downstream `result["typo"]` after the chain fires D0030; `result["literal_key"]` after `set_index([keys])` fires D0030. **Adopters who incorrectly accessed non-aggregate columns after `groupby.agg().reset_index(drop=True)`, or who accessed literal keys after `set_index([keys])`, will see new D0030 fires** — align downstream code with the synthesized schema, or access keys via `.index` (not modeled; `reset_index(drop=False)` is a v1.17 deferral). The v1.14 synthesis-arm cross-codebase coverage gaps close in [pykrete-tests PR-P1 #50](https://github.com/amirnaderi93/pykrete-tests/pull/50) with positive probes against `groupby.agg` on real-library fixtures + negative probes against D0080 constructor returns. The dtype-override family (the inference table shared between `pivot_table(aggfunc=)` and `groupby.agg`) consolidates behind the new `resolve_override_ty` primitive in preparation for the window-aggregation arms (the `Windowed` lattice variant was evaluated and rejected in v1.16 in favor of direct-chain recognition; deferred to v1.17). The audit-tooling block gains **marketing-table gate v3** — `scripts/trust-claim-sweep-checklist.sh` now scans markdown-table cells of shape `<num> <key>` and requires each to match either a current `text-numeric` pin OR a `text-numeric-historical` block in CHANGELOG (the v1.14 carve-out on `docs-site/src/content/docs/about/pandas-roadmap.md` v1.3-v1.8 trajectory pins resolves via the v1.15 PR-F text-numeric-historical backfill). Cross-codebase coverage: `299` → `305 probes` across `164 fixtures` from `17 donors`.

**New in v1.16: time/window aggregation + dict & callable `groupby.agg` + honest-silence declines + `inplace=` guards.** `df.resample("<rule>").agg("<fn>")` synthesizes a `Derived` schema at the aggregate-driven dtype (`count` / `nunique` → Long; `mean` / `std` / `var` / `median` → Double; `sum` / `min` / `max` / `first` / `last` preserve the receiver column's dtype). `df.rolling(<n>).agg("<fn>")` synthesizes all-Double — pandas rolling upcasts numeric columns to `float64` regardless of the aggregate — and only when every receiver column is numeric; rolling aggfuncs are restricted to `count` / `sum` / `mean` / `std` / `var` / `median` / `min` / `max`. Both arms recognize a single expression only: direct-method `df.resample("M").sum()`, held-intermediate chains (`r = df.resample("D")` then `r.agg(...)`), non-string aggfuncs, and non-literal rule / window arguments all fall through silently. `groupby.agg` gains its dict form (keeping **only** the named columns plus group keys — unnamed receiver columns are dropped) and its callable form (`np.mean` / `len`, which on an all-numeric frame keeps keys plus every non-key column at Unknown dtype). Four shapes now decline to Unknown where the checker previously over-claimed: named-aggregation `groupby(k).agg(out=(col, fn))` (a **false positive** through v1.15 — it synthesized a keys-only schema and fired a bogus `returnColumnsMismatch` on correct code), `rolling` over any non-numeric column, numeric-restricting aggregation over a frame carrying a non-numeric column (on `groupby.agg` and `resample.agg` alike), and `resample(<rule>, on=<col>)`. The rationale is the same in the first three: pandas 2.x **raises** (`TypeError` / `DataError`) rather than silently dropping the column the way pre-2.0 pandas did, and pykrete does not synthesize a schema for code that errors at runtime. The fourth is the same principle applied to shape rather than dtype — pandas moves the `on=` column into the resample index, so keeping it claimed a column the result doesn't have; `rolling(<n>, on=<col>)` keeps the column and is deliberately unaffected. `reset_index(inplace=True)` / `set_index(inplace=True)` resolve to `None`, matching pandas, with D0030 key-existence still validated before the inplace punt. **Adopters who accessed columns dropped by a dict-form `groupby.agg`, or who chained off an `inplace=True` reset/set result, will see new D0030 fires.** Cross-codebase coverage: `305` → `312 probes` across `171 fixtures` from `17 donors` — **the window-aggregation arms have no cross-codebase probes; they are unit-tested in-crate, with donor coverage tracked for v1.17.** The `SchemaView::Windowed` lattice variant was evaluated and rejected this cycle in favor of direct-chain recognition.

CI fails if any probe asserts the wrong outcome. The probe runner and marker grammar are documented in [`scripts/PROBES.md`](https://github.com/amirnaderi93/pykrete-tests/blob/main/scripts/PROBES.md).

What the v1.16 probe suite does **not** yet verify, all tracked for v1.17+:

- **`pivot_table` multi-aggregate aggfunc list** (e.g., `aggfunc=['sum', 'mean']`) — v1.13 lands single-aggfunc `Derived` schema synthesis; the multi-aggfunc MultiIndex result inference is deferred.
- **Full `melt` / `stack` / `unstack` output schema-tracking** — the wide / long output schemas (`var_name` / `value_name` become columns of the long frame for `melt`; index-level promotion / demotion for `stack` / `unstack`). v1.6 + v1.7 + v1.10 + v1.11 ship literal-form column checking on the inputs; v1.13 ships pivot_table `Derived` synthesis; v1.14 ships `groupby.agg` Derived synthesis; v1.15 ships `reset_index(drop=True)` + `set_index([literal-keys])` chain-depth pass-through; the remaining output-shape models pair with the rest of pandas reshape in v1.17+.
- **`.loc[mask, "col"]` (boolean mask) row-key tracking, `.loc[:, "a":"b"]` (column range), `pdf.iloc[...]`** — carried forward to v1.17+, paired with broader pandas reshape.
- **`df.query("…")` / `df.eval("…")` mini-DSLs.** Own design surface; numexpr-influenced syntax, separate parser from the SQL path used by `selectExpr`.
- **Cross-codebase probe coverage for the v1.16 window-aggregation arms.** `resample.agg`, `rolling.agg`, and the dict-form / callable `groupby.agg` shapes are unit-tested in-crate only; no donor fixture exercises them yet.
- **Broader pandas method modeling** (`reset_index(drop=False)` index-as-column promotion, `set_index(<expr>)` non-literal forms, `expanding.agg`, named-aggregation `groupby(k).agg(out=(col, fn))` precise modeling, dict / list / callable aggfuncs on window chains, and held-intermediate window chains — `stack` shipped in v1.10, `unstack` in v1.11, `pivot_table(aggfunc=)` Derived synthesis in v1.13, `groupby.agg` single-string Derived synthesis in v1.14, `reset_index(drop=True)` + `set_index([literal-keys])` literal-form chain-depth in v1.15, `resample.agg` + `rolling.agg` direct chains + dict-form and callable `groupby.agg` in v1.16).
- **`pd.read_csv(...)` and other pandas I/O entry points.** Schema inference from file headers / SQL / type-stubs is a separate design surface.
- **`PROBE-TYPE-IS` synth-shape coverage beyond D0081 (Spark side).** The current synth (`{df}.select(col("x") + 1)`) falsifies on non-numeric. D0082 (`crossTypeComparison`) still needs its own synth shape; raw-mutation fixtures cover it in the interim. (D0080 `returnTypeMismatch` cross-codebase coverage shipped in v1.12 per pykrete-tests PR-P1 #42; v1.13 shipped the dialect-on-return checker arm; v1.14 closes the constructor carve-out; v1.15 closes the v1.14 synthesis-arm cross-codebase coverage gap on `groupby.agg` and D0080 constructor returns per pykrete-tests PR-P1 #50.)
- Numeric-subtype distinguishability (`int` vs `long` vs `short` arithmetic narrowing). Carried forward from v1.1.
- **withColumn output enum-constraint preservation.** Carried forward from v1.1.

The v1.16 release lifts probe coverage from `305` → `312 probes` and fixture coverage from `164` → `171 fixtures` via the v1.15-mandated `reset_index(drop=True)` + `set_index([key])` cross-codebase chain probes plus `groupby.agg` donor widening shipped in pykrete-tests PR-P1 #54 — those probes verify column-name tracking (that the chain stays non-Unknown), with dtype-correctness carried by the golden-diff suite, not the probe suite. The v1.16 window-aggregation arms (`resample.agg` / `rolling.agg` / dict-form + callable `groupby.agg`) have no cross-codebase probes yet — they are unit-tested in-crate, deferred to v1.17. The v1.15 release lifted probe coverage from `299` → `305 probes` and fixture coverage from `158` → `164 fixtures` via the v1.15 PR-P1 synthesis-arm cross-codebase coverage batch (positive `groupby.agg` + negative D0080 constructor returns) shipped in pykrete-tests PR-P1 #50. The v1.14 release lifted probe coverage from `289` → `299 probes` and fixture coverage from `148` → `158 fixtures` via the v1.14 PR-P1 D0080 cross-codebase coverage batch. The v1.13 release shipped no new pykrete-tests batch via PR-P1; the `279` → `289 probes` lift was from in-checker D0080 dialect-on-return + pivot_table aggfunc Derived-schema arms exercised at fixture-level via the v1.13 PR-P1 cross-codebase probes (catalog-pinned alongside v1.14). The v1.12 probe batch ([pykrete-tests PR-P1 #42](https://github.com/amirnaderi93/pykrete-tests/pull/42)) added D0080 `returnTypeMismatch` cross-codebase probes — the longest-standing trust gap since v1.6 closed. The v1.11 probe batch ([pykrete-tests PR-P1 #39](https://github.com/amirnaderi93/pykrete-tests/pull/39)) added the cross-codebase property probes for the v1.10 PR-D1 D0091 8-property surface (`na`, `write`, `writeStream`, `storageLevel`, `index`, `values`, `shape`, `T`). The v1.10 probe batch ([pykrete-tests PR-P1 #34](https://github.com/amirnaderi93/pykrete-tests/pull/34)) added 6 negative D0091 probes (2 strict-mode escalation on `mlflow` + `dbt-spark`, 2 bare-attribute on `pandera` + `delta`, 2 tightened with `match /note arg shape differs/`), and [pykrete-tests #35](https://github.com/amirnaderi93/pykrete-tests/pull/35) added the seaborn `stack(level=)` arm. The v1.9 cycle's D0091 batch ([pykrete-tests PR-P1 #32](https://github.com/amirnaderi93/pykrete-tests/pull/32)) added 2 negative D0091 probes (pandera Pandas→Spark misuse + delta Spark→Pandas misuse). The v1.8 cycle's D0073 / D0083 sweep ([pykrete-tests PR-P1 #30](https://github.com/amirnaderi93/pykrete-tests/pull/30)) and the v1.7 cycle's pandas `melt` probe pair ([pykrete-tests PR-D1 #28](https://github.com/amirnaderi93/pykrete-tests/pull/28)) plus the D0040 / D0050 / D0051 sweep ([pykrete-tests PR-P1 #27](https://github.com/amirnaderi93/pykrete-tests/pull/27)) all landed in prior catalog windows.

Gaps closed in earlier releases (`df["X"]` subscript, GroupedData shortcut aggregates, chained nested-field access, `intersect` / `subtract` / `exceptAll`, lowercase `groupby`) all have regression tests in `crates/pykrete/tests/`. They can't reopen silently.

## Known limitations

By design, pykrete does not model:

- **Structured streaming** (`readStream`, `writeStream`, `isStreaming`).
- **RDD-level operations** (`rdd`, `mapPartitions`, `foreach`).
- **Cross-dialect handoffs beyond `.toPandas()` and `spark.createDataFrame(pdf)`.** v1.5 closed the two principal handoff paths (`SparkFrame[X]` → `PandasFrame[X]` via `.toPandas()`, and `PandasFrame[Y]` → `SparkFrame[Y]` via `spark.createDataFrame(pdf)` when a schema source is present); v1.6 closed the matching `.take()` dialect-gate on the pandas side. Other cross-dialect interop — `.toArrow`, `mapInPandas`, `pandas_api`, `applyInPandas` — remains opaque by design; these cross into pandas-on-Spark / Arrow tables, not vanilla pandas. See the [roadmap](/about/roadmap/) and the [pandas roadmap](/about/pandas-roadmap/) for the pandas-specific trajectory.

The full unmodeled list, with the rationale for each, is in [Operations → What's not modeled — by design](/reference/operations/#whats-not-modeled--by-design).

## Production deployments

Pykrete is a development-time checker — it does not ship to production hosts and cannot affect a running pipeline. The public, reproducible cross-testing coverage lives in [pykrete-tests](/about/pykrete-tests/), which vendors `171 fixtures` (58 annotated + 113 deliberately-corrupted under `probes_negative/`) across `17 donors` (10 PySpark + 10 pandas-coverage, three of which are hybrid carry-overs from v1.3); the `312 probes` that run on every release cover the probe-anchored fixtures of those 171 (`195 positive` + `117 negative`). Each fixture is pinned to a specific upstream commit (see the [per-donor matrix](https://github.com/amirnaderi93/pykrete-tests#the-donors) in the pykrete-tests README) so the coverage is reproducible — anyone can `pip install` the same upstream code pykrete is being tested against. Named external adopter references will be added here as teams give the go-ahead.

See the [Reliability and trust](https://github.com/amirnaderi93/pykrete#reliability-and-trust) section of the README for the full story.
