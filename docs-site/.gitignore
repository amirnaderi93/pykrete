# Astro / Node
node_modules/
dist/
.astro/

# Editor
.vscode/
.DS_Store

# Lockfile policy: committed at the repo root level if/when CI repro
# matters more than ease of local install. For now, omit so npm/pnpm/
# yarn users can pick their tooling without conflicting lockfiles.
package-lock.json
pnpm-lock.yaml
yarn.lock

# pyright-browser worker is copied from node_modules by
# scripts/copy-pyright-worker.mjs at dev/build time. Don't commit it.
public/pyright.worker.js
